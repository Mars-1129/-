1、项目立项说明书
文档作用：双人统一项目核心认知，杜绝需求蔓延、目标跑偏，是所有开发工作的基准依据
核心内容：
- 项目核心背景、业务价值、解决的核心痛点
- 项目明确边界：核心必做功能、明确不做功能（大型项目核心，防止无限堆需求）
- 双人固定权责分工：人员A（模块1和模块3）、人员B（模块2和模块4）
- 资源清单：服务器、域名、第三方接口、权限、开发环境、预算资源
- 核心背景、业务价值、解决的痛点；项目明确边界（P0/P1/P2功能分级）；双人分工
  
2、产品需求文档
核心内容：业务整体流程图、各模块功能需求、用户场景、交互逻辑、异常场景、权限规则、需求优先级。双人专属优化：精简冗余描述，重点标注双人分工对应的功能模块、业务流程图、三模块功能需求、用户场景、交互逻辑、异常场景、P0/P1/P2优先级标注 业务整体流程图、各模块功能需求、用户场景、交互逻辑、异常场景、权限规则、需求优先级。双人专属优化：精简冗余描述，重点标注双人分工对应的功能模块

3、整体系统架构设计文档
文档作用：统一两人技术思路，规范大型项目整体架构，避免技术方案冲突、后期重构
核心内容：
- 整体架构拓扑图、分层设计（前端层、服务层、数据层、AI Agents智能层、接口层）
- 全套技术栈选型及版本锁定（前后端框架、数据库、中间件、AI框架）
- 业务模块拆分、模块独立职责、跨模块调用关系
- 性能设计、并发处理、安全设计、容错降级方案
- 大型项目技术难点、预研结论、风险规避方案
整体架构拓扑图、分层设计、全套技术栈与版本锁定、模块拆分与跨模块调用关系、性能/并发/容错设计
- 整体架构拓扑图、分层设计（前端层、服务层、数据层、AI Agents智能层、接口层）
- 全套技术栈选型及版本锁定（前后端框架、数据库、中间件、AI框架）
- 业务模块拆分、模块独立职责、跨模块调用关系
- 性能设计、并发处理、安全设计、容错降级方案
- 大型项目技术难点、预研结论、风险规避方案

4、 数据库设计文档
文档作用：统一数据结构，是大型项目数据稳定的核心根基
核心内容：ER实体关系图、全量表结构（字段、类型、主键、索引、注释）、数据字典、枚举定义、数据约束、备份策略、数据同步规则完整Prisma Schema（字段/类型/主键/外键/索引/枚举/关系）ER实体关系图、全量表结构（字段、类型、主键、索引、注释）、数据字典、枚举定义、数据约束、备份策略、数据同步规则

5、 全局API接口文档
核心内容：全局请求/响应规范、统一错误码、所有业务接口入参出参、校验规则、示例报文、接口权限、限流规则、AI模块专属接口、全局请求/响应规范、统一错误码、所有业务接口入参出参、校验规则、示例报文、接口权限、限流规则、AI模块专属接口，完整清单（方法/端点/赛题级别/核心文件映射）；SSE事件类型 

6、素材模块接口文档 
| 全局请求/响应规范、统一错误码、所有业务接口入参出参、校验规则、示例报文、接口权限、限流规则、AI模块专属接口 | 开发素材功能时 |

7、剧本模块接口 文档
  全局请求/响应规范、统一错误码、所有业务接口入参出参、校验规则、示例报文、接口权限、限流规则、AI模块专属接口 | 开发剧本功能时 |

8、 创作模块接口文档 
全局请求/响应规范、统一错误码、所有业务接口入参出参、校验规则、示例报文、接口权限、限流规则、AI模块专属接口| 开发创作功能时 |

9、分析看板接口文档 
全局请求/响应规范、统一错误码、所有业务接口入参出参、校验规则、示例报文、接口权限、限流规则、AI模块专属接口 | 开发看板功能时 |

10. 代码规范与开发规约文档
核心内容：命名规范、注释规范、代码提交规范、分支管理策略、合并冲突处理规则、双人代码分工规范，保证两人代码风格统一、可维护、分支管理策略（main/feat/fix）、PR+CodeReview流程 命名规范、注释规范、代码提交规范、分支管理策略、合并冲突处理规则、双人代码分工规范，保证两人代码风格统一、可维护

11、AI Agents专属文档
| Agents 目录摘要 |  全局摘要、技术栈速览、三大模块能力映射表（功能需求→文件→函数）、数据分析模块能力映射 | AI/开发者介入项目的第一步 |
| Agents 详细设计 | 
| Agents 层级关系 |
| 全局公共配置 | API限流参数、VRAM预算、环境变量 | 配置调整时 
| 素材模块 | 多模态视网膜 + 原子内容弹药库 | 上传(P0)/切片(P0)/打标(P0)/向量(P0)/混合检索(P1)/SFX提取(P2) | 人员 A |
| 剧本模块 | 智能增强中间层 | 快速模式(P0)/仿写(P1)/模板(P1)/Schema校验(P0)/音节配时(P1)/合规(P2) | 人员 B |
| 创作模块 | 生产核心车间 | 一键成片(P0)/四轨合成(P0-P1)/增量拼接(P1)/响度标准化(P2)/同构渲染(P2) | 人员 A |
| 数据分析 | 归因看板 | 热力图(P2)/桑基图(P2)/留存曲线(P1)/AB对比(P2)/一键自愈(P2) | 人员 B |

12、整体测试用例文档
核心内容：业务功能用例、接口测试用例、AI Agents智能能力测试用例、边界场景、异常场景、权限场景全覆盖，| 测试用例文档 | `TikStream_AI.md` 各模块末尾 | 素材/剧本/创作各模块测试用例（输入条件/预期结果/核验阻断点）业务功能用例、接口测试用例、AI Agents智能能力测试用例、边界场景、异常场景、权限场景全覆盖 | 编写测试时 |
业务功能用例、接口测试用例、AI Agents智能能力测试用例、边界场景、异常场景、权限场景全覆盖| 运行端到端测试时 |

13、 Bug台账与回归测试文档
核心内容：问题描述、复现步骤、严重等级、归属修复人、修复状态、回归结果、遗留问题记录、

14. 环境搭建与部署文档
核心内容：本地开发环境、测试环境、生产环境搭建步骤、依赖安装、环境变量配置、项目打包、编译、启动、关停命令，本地开发环境、测试环境、生产环境搭建步骤、依赖安装、环境变量配置、项目打包、编译、启动、关停命令 ，|火山方舟API Key（赛题指定，请勿泄漏）、数据库连接、API限流参数 | 部署前配置 |启动步骤、 CI/CD 流水线 

15. 运维与故障排查文档
核心内容：日志路径、监控指标、日常运维流程、常见故障排查方案、服务重启、版本回滚、数据恢复、AI服务异常处理方案
